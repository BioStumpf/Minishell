echo - should return a -

cd noexistdir: exit status (echo $?) is 0 for us should be 1

export --TEST=123 -> allowed varnames: alphanumeric chars + _ (underscores) no -/ whatever allowed
export TE-ST=123 -> allowed varnames: alphanumeric chars + _ (underscores) no -/ whatever allowed

double free:
minishell$ /bin/echo $HOME
/home/david
minishell$ unset HOME
minishell$ unset HOME
minishell$ unset HOME
minishell$ /bin/echo $HOME
minishell: /bin/echo: command not found
double free or corruption (out)
minishell$ bash
minishell: bash: command not found
double free or corruption (out)
minishell$ echo hi
hi
minishell$ bash
minishell: bash: command not found
double free or corruption (out)

exit "" -> should prompt that numeric argument is required and exit with code 2
exit 1 2 -> should not exit, print too many arguments and exit with 1

input redir just exits the shell even though it shouldnt:
minishell$ <out
minishell: No such file or directory
david@davidslaptop:~/Documents/coding/Minishell$ echo $?
1

should return 126 (error message also should be different but return code should definetly match)
minishell$ /bin/
minishell: Permission denied
minishell$ echo $?
1
same for:
.
..
/

->return code should be 126 here

should return 127 (error mesage is permission denied and should be at least command not found (its actually sth completely different but i guess command not found seems reasonable))
minishell$ "."
minishell: Permission denied
minishell$ echo $?
1

pwd doesnt change working directory
mkdir "tmp_test_folder"
/bin/echo $PWD
cd "tmp_test_folder"
/bin/echo $PWD
cd ..
/bin/echo $PWD
rm -rf "tmp_test_folder"

double free:
david@davidslaptop:~/Documents/coding/Minishell$ ./minishell
minishell$ export NEW=42
minishell$ env | grep NEW
NEW=42
minishell$ unset NEW
minishell$ env | grep NEW
minishell$ unset NEW
minishell$ unset NEW
minishell$ unset NEW
minishell$ unset NEW
minishell$ unset NEW
minishell$ env | grep NEW
minishell: grep: command not found
double free or corruption (out)

here the return code is 139? also ls should still print the contents of the folder dunno whats happening here maybe we pass the wrong env pointer to execve and ls is confused? check what we pass execve incase of unsetting PATH
also in general the PATH FAILS category inside the tester seems to be not working 100% (unset PATH seems to misbehave generally, so take a look at PATH FAILS category)
 ./minishell
minishell$ unset PATH
minishell$ cd /bin
minishell$ ls
minishell: Success


